[defaults]
# Include primary roles directory plus nested shared infrastructure roles
roles_path = src/roles:src/roles/infrastructure/shared
# Use YAML result formatting without deprecated community.general.yaml callback
stdout_callback = default
bin_ansible_callbacks = True

[callback_default]
result_format = yaml
