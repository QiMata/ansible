---
dependency:
  name: galaxy
driver:
  name: delegated
lint: |
  ansible-lint --offline ../../src/playbooks/data_product.yml
platforms:
  - name: localhost
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
scenario:
  name: data_product
verifier:
  name: ansible
