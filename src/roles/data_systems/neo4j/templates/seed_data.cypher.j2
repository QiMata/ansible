# Data Seeding Script for {{ item.name }}
# Generated by Ansible

{% for statement in item.statements | default([]) %}
{{ statement }};
{% endfor %}

{% if item.create_constraints | default(false) %}
// Create constraints
{% for constraint in item.constraints | default([]) %}
{{ constraint }};
{% endfor %}
{% endif %}

{% if item.create_indexes | default(false) %}
// Create indexes
{% for index in item.indexes | default([]) %}
{{ index }};
{% endfor %}
{% endif %}
