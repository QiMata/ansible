---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include elasticsearch_snapshot role"
      ansible.builtin.include_role:
        name: elasticsearch_snapshot
