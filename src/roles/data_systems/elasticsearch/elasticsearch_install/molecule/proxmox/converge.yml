---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include elasticsearch_install role"
      ansible.builtin.include_role:
        name: elasticsearch_install
