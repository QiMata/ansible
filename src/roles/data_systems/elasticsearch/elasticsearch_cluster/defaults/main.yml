---
elasticsearch_cluster_version: "{{ elasticsearch_version | default('8.x') }}"
elasticsearch_cluster_name: "amundsen-es-cluster"
elasticsearch_cluster_heap_size: "{{ elasticsearch_heap_size | default('2g') }}"
elasticsearch_cluster_node_roles: ["master", "data"]
elasticsearch_cluster_network_host: "{{ elasticsearch_network_host | default('0.0.0.0') }}"
