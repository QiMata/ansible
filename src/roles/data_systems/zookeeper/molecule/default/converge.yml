---
- name: Converge
  hosts: zookeeper_nodes
  become: true
  vars:
    zookeeper_nodes: "{{ groups['zookeeper_nodes'] | default([]) | list }}"
    zookeeper_client_port: 2181
  roles:
    - role: zookeeper
