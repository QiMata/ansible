---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true

- name: Restart Kibana
  ansible.builtin.service:
    name: "{{ kibana_service_name }}"
    state: restarted
    enabled: true
