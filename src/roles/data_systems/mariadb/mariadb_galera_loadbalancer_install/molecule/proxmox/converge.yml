---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include mariadb_galera_loadbalancer_install role"
      ansible.builtin.include_role:
        name: mariadb_galera_loadbalancer_install
