---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include postgresql_monitoring role"
      ansible.builtin.include_role:
        name: postgresql_monitoring
