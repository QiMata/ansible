---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart postgres_exporter
  ansible.builtin.systemd:
    name: postgres_exporter
    state: restarted

- name: restart postgresql
  ansible.builtin.systemd:
    name: postgresql
    state: restarted
