---
galaxy_info:
  role_name: airflow_connector
  namespace: data_analytics
  author: "Ansible Automation"
  description: "Ansible role for managing Apache Airflow connections"
  company: "QiMata Technologies"
  license: "MIT"
  min_ansible_version: "2.9"

  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy
    - name: Debian
      versions:
        - buster
        - bullseye
        - bookworm

  galaxy_tags:
    - airflow
    - apache
    - connections
    - data
    - workflow
    - orchestration
    - etl

dependencies: []  # This role assumes Apache Airflow is already installed

collections:
  - ansible.builtin
