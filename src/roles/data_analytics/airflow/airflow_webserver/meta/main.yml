---
galaxy_info:
  role_name: airflow_webserver
  namespace: data_analytics
  author: "Ansible Automation"
  description: "Ansible role for managing Apache Airflow webserver service"
  license: "MIT"
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy
    - name: Debian
      versions:
        - buster
        - bullseye

dependencies:
  - role: apache_airflow
    when: airflow_webserver_require_core | default(true)
