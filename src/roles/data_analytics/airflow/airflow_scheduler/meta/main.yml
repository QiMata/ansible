---
galaxy_info:
  role_name: airflow_scheduler
  namespace: data_analytics
  author: "Ansible Automation"
  description: "Ansible role for managing Apache Airflow scheduler service"
  license: "MIT"
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy
    - name: Debian
      versions:
        - buster
        - bullseye

dependencies:
  - role: apache_airflow
    when: airflow_scheduler_require_core | default(true)
