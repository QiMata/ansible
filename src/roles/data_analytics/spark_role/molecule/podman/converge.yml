---
- name: Converge
  hosts: all
  become: true
  vars:
    ansible_roles_path: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') }}/src/roles"
  roles:
    - role: spark_role
      vars:
        spark_role_history_enabled: true
