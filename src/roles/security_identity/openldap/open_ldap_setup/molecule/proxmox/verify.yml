---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Role open_ldap_setup verification passed"
        fail_msg: "Role open_ldap_setup verification failed"
