---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include openldap_password_policies role"
      ansible.builtin.include_role:
        name: openldap_password_policies
