---
- name: Restart Vault
  ansible.builtin.service:
    name: "{{ vault_service_name }}"
    state: restarted

- name: Restart Rsyslog
  ansible.builtin.service:
    name: rsyslog
    state: restarted
