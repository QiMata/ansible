---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Role keycloak_realm verification passed"
        fail_msg: "Role keycloak_realm verification failed"
