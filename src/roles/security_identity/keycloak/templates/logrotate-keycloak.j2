/var/log/keycloak/*.log {
    daily
    rotate 30
    missingok
    notifempty
    compress
    delaycompress
    copytruncate
    create 0644 {{ keycloak_user }} {{ keycloak_user }}
    postrotate
        systemctl reload keycloak
    endscript
}
