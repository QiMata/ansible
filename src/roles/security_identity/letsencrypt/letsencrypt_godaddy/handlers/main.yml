---
- name: Reload service
  listen: reload_service
  ansible.builtin.command: "{{ __letsencrypt_godaddy_service_reload_command }}"
  when: __letsencrypt_godaddy_service_reload_command | length > 0
  become: true
  changed_when: false
