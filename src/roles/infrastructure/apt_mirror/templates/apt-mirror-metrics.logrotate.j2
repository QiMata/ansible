/var/log/apt-mirror-metrics/*.log {
    rotate 7
    daily
    compress
    missingok
    notifempty
    create 0644 root root
    sharedscripts
    postrotate
        systemctl reload-or-restart apt-mirror-metrics >/dev/null 2>&1 || true
    endscript
}
