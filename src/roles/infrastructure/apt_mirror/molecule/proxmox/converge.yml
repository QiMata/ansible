---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include apt_mirror role"
      ansible.builtin.include_role:
        name: /ansible/src/roles/infrastructure/apt_mirror
