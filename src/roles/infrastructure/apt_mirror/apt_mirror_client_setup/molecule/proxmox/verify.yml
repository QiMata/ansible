---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Role apt_mirror_client_setup verification passed"
        fail_msg: "Role apt_mirror_client_setup verification failed"
