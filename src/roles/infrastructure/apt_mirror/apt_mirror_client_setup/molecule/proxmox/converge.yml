---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include apt_mirror_client_setup role"
      ansible.builtin.include_role:
        name: apt_mirror_client_setup
