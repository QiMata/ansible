---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include remove_unnecessary_packages role"
      ansible.builtin.include_role:
        name: remove_unnecessary_packages
