---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include base role"
      ansible.builtin.include_role:
        # Use simple role name; roles_path includes infrastructure/shared
        name: base
