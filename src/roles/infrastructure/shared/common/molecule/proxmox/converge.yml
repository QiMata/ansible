---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include common role"
      ansible.builtin.include_role:
        # Use absolute path to avoid role resolution issues inside Molecule scenario
        name: /ansible/src/roles/infrastructure/shared/common
