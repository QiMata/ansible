---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include ha_proxy_load_balancer_setup role"
      ansible.builtin.include_role:
        name: ha_proxy_load_balancer_setup
