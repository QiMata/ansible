---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Role ha_proxy_load_balancer_setup verification passed"
        fail_msg: "Role ha_proxy_load_balancer_setup verification failed"
