---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: logstash
    image: debian:12
    privileged: true
    command: "/sbin/init"
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
verifier:
  name: testinfra
