# Example legacy datasource definition used when templated configuration is unavailable.
# Customize this file to match the datasources bundled with the role.
apiVersion: 1
datasources:
  - name: Example Prometheus
    type: prometheus
    url: http://prometheus:9090
    access: proxy
    isDefault: true
