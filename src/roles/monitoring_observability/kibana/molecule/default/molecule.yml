---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: kibana
    image: debian:12
    privileged: true
    command: "/sbin/init"
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
verifier:
  name: testinfra
