---
- name: Converge
  hosts: all
  become: true
  gather_facts: true
  tasks:
    - name: "Include configure_filebeat_os role"
      ansible.builtin.include_role:
        name: configure_filebeat_os
