---
dependency:
  name: galaxy
  enabled: false
driver:
  name: default
  options:
    managed: false
    ansible_connection_options:
      ansible_connection: ssh
platforms:
  - name: jenkins_agent-test-instance
    groups:
      - test_instances
      - jenkins_agent_test
provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: >-
      /ansible/src/roles:/ansible/src/roles/devops_cicd:/ansible/src/roles/devops_cicd/jenkins
  playbooks:
    create: ../../../../../../molecule/proxmox/create.yml
    prepare: ../../../../../../molecule/proxmox/prepare.yml
    converge: converge.yml
    verify: verify.yml
    destroy: ../../../../../../molecule/proxmox/destroy.yml
  inventory:
    host_vars:
      jenkins_agent-test-instance:
        ansible_host: "{{ lookup('env', 'CONTAINER_IP') | default('10.80.0.200') }}"
        ansible_user: molecule
        ansible_connection: ssh
        ansible_ssh_pass: molecule123
        ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
        ansible_become: true
        ansible_become_method: sudo
  config_options:
    defaults:
      host_key_checking: false
      stdout_callback: yaml
      bin_ansible_callbacks: true
      role_name_check: false
  roles_path: "/ansible/src/roles:/ansible/src/roles/devops_cicd:/ansible/src/roles/devops_cicd/jenkins"
verifier:
  name: ansible
scenario:
  name: proxmox
  test_sequence:
    - dependency
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - side_effect
    - verify
    - cleanup
    - destroy
