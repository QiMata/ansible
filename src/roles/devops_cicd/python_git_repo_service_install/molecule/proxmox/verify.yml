---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: Example assertion
      ansible.builtin.assert:
        that:
          - true
        success_msg: "Role python_git_repo_service_install verification passed"
        fail_msg: "Role python_git_repo_service_install verification failed"
