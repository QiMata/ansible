---
dependency:
  name: galaxy
driver:
  name: delegated
  options:
    managed: false
platforms:
  - name: localhost
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
verifier:
  name: ansible
