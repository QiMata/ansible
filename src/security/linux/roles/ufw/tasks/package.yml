---
# Install the packages
- name: "Package: install packages"
  ansible.builtin.apt:
    name: "{{ ufw_packages }}"
    state: present
  become: true
  register: ufw_package_result
  until: ufw_package_result is successful
  tags: usw, package
