filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/neo4j/*.log
    fields:
      service: neo4j
      cluster: "{{ 'cluster' if neo4j_cluster_enabled else 'single' }}"

output.logstash:
  hosts: ["{{ neo4j_elk_host }}:{{ neo4j_elk_port }}"]

