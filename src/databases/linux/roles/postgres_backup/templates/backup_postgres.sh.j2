#!/bin/bash
set -euo pipefail
IFS=$'\n\t'

# Configuration
DB_USER="{{ db_user }}"
DB_NAME="{{ db_name }}"
BACKUP_DIR="{{ backup_dir }}"

# Timestamp
TIMESTAMP=$(date +"%F")

# Backup
pg_dump -U $DB_USER $DB_NAME | gzip > $BACKUP_DIR/$DB_NAME-$TIMESTAMP.sql.gz
